if [ -d $HOME/.cloud-sdk ]; then

	# The next line updates PATH for the Google Cloud SDK.
#	source $HOME/.cloud-sdk/path.bash.inc

	# The next line enables bash completion for gcloud.
#	source $HOME/.cloud-sdk/completion.bash.inc
fi
